PORT=3003
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASS=REPLACE_ME
DB_NAME=majadigi_main

REDIS_URL=redis://localhost:6379

ADMIN_SECRET_KEY=REPLACE_ME

# FCM HTTP v1 — diperlukan untuk push notification
# FCM_PROJECT_ID: Firebase project ID (lihat Firebase Console > Project Settings)
# FCM_CREDENTIALS_JSON: isi konten service account JSON (bukan path file)
FCM_PROJECT_ID=REPLACE_ME
FCM_CREDENTIALS_JSON=REPLACE_ME
